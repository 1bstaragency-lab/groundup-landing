/**
 * Temporary kill-switch for live registration while building pre-launch
 * hype/anticipation. When true, every "create an account" CTA across the
 * site routes to the waitlist instead of live signup/checkout.
 *
 * Flip back to false to restore normal live registration.
 */
export const WAITLIST_MODE = true
